<?xml version="1.0"?>
<ruleset name="Stripe Payment Module">
    <description>PHP CodeSniffer configuration for Stripe Payment Module</description>

    <!-- Use PSR-12 coding standard -->
    <rule ref="PSR12">
        <!-- Exclude underscore prefix rule - OXID framework convention -->
        <exclude name="PSR2.Classes.PropertyDeclaration.Underscore"/>
    </rule>

    <!-- Paths to check -->
    <file>../src</file>

    <!-- Exclude vendor and other directories -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/var/*</exclude-pattern>

    <!-- Show progress -->
    <arg value="p"/>

    <!-- Use colors in output -->
    <arg name="colors"/>

    <!-- Show sniff codes in all reports -->
    <arg value="s"/>
</ruleset>
